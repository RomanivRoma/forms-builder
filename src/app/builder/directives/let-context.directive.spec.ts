import { LetContextDirective } from './let-context.directive';

describe('LetContextDirective', () => {
  it('should create an instance', () => {
    // const directive = new LetContextDirective();
    // expect(directive).toBeTruthy();
  });
});
