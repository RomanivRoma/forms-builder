import { StyleTransformPipe } from './style-transform.pipe';

describe('StyleTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new StyleTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
